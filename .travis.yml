sudo: required
services:
    - docker
    
before_install:
    - docker build -t aniketsharma0211/docker-react -f Dockerfile.dev .
    
script:
    - docker run -e CI=true aniketsharma0211/docker-react npm run test
    